import styled from 'styled-components';

export const TodoListWrapper = styled('div')({
    backgroundColor: '#8dc174',
    width: 280,
    marginTop: 30,
    border: '1px solid #2d8d7d',
    borderRadius: 15,
    padding: 30,
    display: 'flex',
    justifyContent: 'space-between',
    flexDirection: 'column'
})