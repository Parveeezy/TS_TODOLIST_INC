import styled from 'styled-components';

export const NewTodoListWrapper = styled('div')({
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    gap: 10,
    color: '#ffffff',
});

export const NewTodoListTitle = styled('h1')({
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    color: '#ffffff'
});